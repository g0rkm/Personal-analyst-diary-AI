[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    qt: PyQt6 arayüzü gerektiren testler (sanal/offscreen ekranla çalışır)
    slow: yavaş çalışan testler (embedding modeli indirir vb.)
